{
  "name": "octagon-farm",
  "version": "1.1.0",
  "private": true,
  "description": "Octagon farm runtime - hub, per-phone session brains, local SQLite",
  "main": "hub.js",
  "scripts": {
    "hub": "node hub.js --slots=4 --duration=60",
    "hub:dry": "node hub.js --slots=4 --duration=60 --test",
    "session": "node farm-brain.js --slot=1 --prefix=Alpha --duration=30 --dry-run --log"
  },
  "dependencies": {
    "better-sqlite3": "~12.11.1"
  },
  "engines": {
    "node": ">=20"
  }
}
