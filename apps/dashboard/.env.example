# Octagon dashboard environment. The app is local-only: no login, no required
# auth variables. Only the database location is configurable.

# Local SQLite file. A relative path resolves from apps/dashboard, so prefer
# an absolute path. The default (repo_root/infra/db/farm.db) works out of the box.
FARM_DB_PATH=../../infra/db/farm.db
