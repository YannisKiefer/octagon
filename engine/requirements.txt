# Core
python-telegram-bot==21.9
httpx==0.27.2

# Video Processing (yt-dlp for scraping)
yt-dlp==2024.12.23

# AI — Gemini Vision for video analysis
google-generativeai==0.8.3

# Numerical / ML (used by SemanticSearch for cosine similarity and k-means clustering)
numpy>=1.26.0

# Utilities
python-dotenv==1.0.1
loguru==0.7.2

# Note: FFmpeg and ExifTool must be installed separately:
#   brew install ffmpeg exiftool
